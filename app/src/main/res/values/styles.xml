<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:textColorTertiary">@color/textColorTertiary</item>
    </style>

    <style name="AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.ActionBar.Popup" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.TextAppearance" parent="TextAppearance.AppCompat" />

    <style name="AppTheme.TextAppearance.Title">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="AppTheme.TextAppearance.TextInputLayoutHint">
        <item name="android:textColor">?android:attr/textColorTertiary</item>
    </style>

    <style name="AppTheme.TextInputLayout">
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="AppTheme.TextInputLayout.ReadOnly">
        <item name="android:focusable">false</item>
        <item name="android:focusableInTouchMode">false</item>
    </style>

    <style name="AppTheme.TextInputEditText" parent="Widget.AppCompat.EditText" />

    <style name="AppTheme.TextInputEditText.ReadOnly">
        <item name="android:focusable">false</item>
        <item name="android:focusableInTouchMode">false</item>
        <item name="android:background">@null</item>
    </style>

</resources>
